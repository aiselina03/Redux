import { createSlice } from '@reduxjs/toolkit'

export const calculatorSlice = createSlice({
  name: 'calculator',
  initialState: {
    value: 0,
  },
  reducers: {
  switch (actions) {
    case value:
        
        break;
  
    
  }
  },
})

export const {add,subsctract,divide,multiple} = calculatorSlice.actions

export default calculatorSlice.reducer